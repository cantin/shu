%h1 Listing books

%table
  %tr
    %th Name
    %th Content abstract
    %th Author abstract
    %th Author
    %th Isbn
    %th Uploader
    %th Upload date
    %th
    %th
    %th

  - @books.each do |book|
    %tr
      %td= book.name
      %td= book.content_abstract
      %td= book.author_abstract
      %td= book.author
      %td= book.ISBN
      %td= book.uploader
      %td= book.upload_date
      %td= link_to 'Show', book
      %td= link_to 'Edit', edit_book_path(book)
      %td= link_to 'Destroy', book, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Book', new_book_path
