= form_for @book_commet do |f|
  -if @book_commet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@book_commet.errors.count, "error")} prohibited this book_commet from being saved:"
      %ul
        - @book_commet.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :commet
    = f.text_area :commet
  .actions
    = f.submit 'Save'
